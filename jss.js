// This function will run when a user selects a risk profile (not needed for the new button, just an example)
function setRiskProfile(profile) {
    alert("You selected the " + profile + " risk profile.");
}
